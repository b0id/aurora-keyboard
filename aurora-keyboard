#!/usr/bin/env bash
# Aurora Touch Keyboard Launcher Script
SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd "$(dirname "$SCRIPT_PATH")" && pwd)"
export PYTHONPATH="$SCRIPT_DIR:$PYTHONPATH"
exec python3 -m aurora_keyboard.main "$@"
